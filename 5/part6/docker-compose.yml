services:
  server:
    image: my_server:v1.0
    container_name: serveR

  nginx:
    image: nginx:latest
    ports:
      - "80:8080"
    volumes:
      - ./nginx2/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - server